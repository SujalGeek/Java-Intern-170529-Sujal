spring:
  application:
    name: monitoring-demo
    profiles:
      dev

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info
          - metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: INFO
    com.example.monitoring_demo: DEBUG



